# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently empty — it contains no source code, build configuration, or documentation yet. This CLAUDE.md was created as the repository's first file.

## Guidance for Future Work

Since no codebase exists yet, there are no build, lint, or test commands to document, and no architecture to describe.

When code is added to this repository, update this file to include:

- **Commands**: how to install dependencies, build, lint, run tests (including a single test), and run the application.
- **Architecture**: the high-level structure and design decisions that require reading multiple files to understand — not an exhaustive file listing.
- **Conventions**: any project-specific conventions (language, framework, formatting tools, branch/PR workflow) once they are established.
